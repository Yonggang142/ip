# Aegis User Guide

Aegis is a playful task-tracking chatbot that helps you manage todos, deadlines, and events from a command-line or JavaFX chat interface.

## Quick Start

1. Ensure you are using **JDK 25**.
2. Run the application:

   ```powershell
   .\gradlew.bat run
   ```

3. Type a command into the input box and press <kbd>Enter</kbd>, or click **Send**.

## Command Summary

| Action | Command format | Example |
| --- | --- | --- |
| Add todo | `todo DESCRIPTION` | `todo borrow book` |
| Add deadline | `deadline DESCRIPTION /by YYYY-MM-DD` | `deadline return book /by 2026-09-20` |
| Add event | `event DESCRIPTION /from YYYY-MM-DD /to YYYY-MM-DD` | `event project meeting /from 2026-09-20 /to 2026-09-21` |
| List tasks | `list` | `list` |
| Mark task | `mark TASK_NUMBER` | `mark 2` |
| Unmark task | `unmark TASK_NUMBER` | `unmark 2` |
| Delete task | `delete TASK_NUMBER` | `delete 3` |
| Find tasks | `find KEYWORD` | `find book` |
| Sort tasks | `sort` | `sort` |
| Exit | `bye` | `bye` |

> [!NOTE]
> Dates must use `YYYY-MM-DD` format. For example, use `2026-09-20`, not `20-09-2026`.

## Features

### Add a Todo

Adds a simple task without a date.

Command format:

```text
todo DESCRIPTION
```

Example:

```text
todo borrow book
```

Expected response:

```text
Ta-da! I added this task:
[T][ ] borrow book
You now have 1 task in your task list.
```

### Add a Deadline

Adds a task that must be completed by a specific date.

Command format:

```text
deadline DESCRIPTION /by YYYY-MM-DD
```

Example:

```text
deadline return book /by 2026-09-20
```

Expected response:

```text
Ta-da! I added this task:
[D][ ] return book (by: Sept 20 2026)
You now have 2 tasks in your task list.
```

### Add an Event

Adds a task with a start date and an end date. The `/from` date must be earlier than the `/to` date.

Command format:

```text
event DESCRIPTION /from YYYY-MM-DD /to YYYY-MM-DD
```

Example:

```text
event project meeting /from 2026-09-20 /to 2026-09-21
```

Expected response:

```text
Ta-da! I added this task:
[E][ ] project meeting (from: Sept 20 2026 to: Sept 21 2026)
You now have 3 tasks in your task list.
```

### List Tasks

Shows all tasks currently saved in your task list.

Command format:

```text
list
```

Expected response:

```text
Here is your current task list:
1.[T][ ] borrow book
2.[D][ ] return book (by: Sept 20 2026)
3.[E][ ] project meeting (from: Sept 20 2026 to: Sept 21 2026)
```

### Mark or Unmark a Task

Marks a task as done, or puts it back in play.

Command formats:

```text
mark TASK_NUMBER
unmark TASK_NUMBER
```

Examples:

```text
mark 1
unmark 1
```

### Delete a Task

Removes a task from the task list.

Command format:

```text
delete TASK_NUMBER
```

Example:

```text
delete 2
```

### Find Tasks

Shows tasks containing the given keyword.

Command format:

```text
find KEYWORD
```

Example:

```text
find book
```

### Sort Tasks

Sorts tasks by date. Todos without dates appear after dated tasks.

Command format:

```text
sort
```

## Error Handling

Aegis tries to catch common mistakes and explain what went wrong:

- Extra spaces and leading or trailing spaces are handled automatically.
- Missing descriptions are rejected.
- Missing or repeated parameters such as `/by`, `/from`, or `/to` are rejected.
- Dates must exist and follow `YYYY-MM-DD` format.
- Event start dates must be earlier than event end dates.
- Duplicate tasks are rejected.
- The `|` character is rejected in user input because it is used internally for storage.

## Data Storage

Aegis saves tasks automatically in:

```text
data/aegis.txt
```

Do not edit this file manually unless you understand the storage format. If the file contains invalid task data, Aegis will start with an empty task list instead of crashing.
